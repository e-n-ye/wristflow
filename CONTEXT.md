# WristFlow 术语

本表只统一手表产品的用语。参数和交互规则见 [交互契约](docs/UI-INTERACTION.md)，表盘设计增量见 [产品设计记录](docs/PRODUCT-DESIGN.md)。

## 显示与输入

**主表盘**：日常查看时间的主页，也是全局按键回主页的目标。
_避免混用_：应用菜单、任意卡片。

**表盘款式**：主表盘的一套外观与信息编排；更换款式只改变主表盘页面。
_避免混用_：整机主题、核心卡片布局。

**扩散表盘**：以完整的大号小时数字为底、中央保持白色实体、整组左右两端各覆盖两层灰色毛玻璃质感的表盘款式；双位小时也作为一个整体。
_避免混用_：逐个数字向内扩散、倾斜片层、等宽彩色竖条切分、呼吸动画。

**表盘选择页**：长按主表盘后进入、用于浏览和切换表盘款式的页面。
_避免混用_：应用菜单、信息环布局选择页。

**表盘样式**：同一款式提供的外观选项，如配色；是否可调整由该款式决定。
_避免混用_：表盘款式、信息内容选择。

**表盘组件**：位于表盘内部、展示某项信息的区域；支持配置的款式可允许用户选择该区域显示的内容。
_避免混用_：左右滑动的信息页、信息环槽位。

**应用菜单**：用于选择并进入应用的全局入口；本轮选择蜂窝形态。
_避免混用_：表盘选择页、控制中心。

**核心卡片环**：主表盘、心率、今日活动和系统状态组成的横向循环页面集合。

**变暗态**：屏幕仍显示内容、亮度相对用户设置降低的过渡状态；首次触摸用于提亮。
_避免混用_：低亮度用户设置、息屏。

**息屏态**：手表停止显示画面的产品状态；此名称不代表主控或整机已达到特定功耗。
_避免混用_：深睡、整机断电。

**返回**：回到当前页面的上一级，并保留该上级页面应恢复的浏览位置。
_避免混用_：回主页。

**回主页**：离开当前页面层级，回到主表盘。
_避免混用_：返回上一级、返回应用菜单。

**通知预览**：新消息到来时临时展示的简短内容，与通知中心及完整详情区分。
