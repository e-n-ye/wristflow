# WristFlow 术语

本表只统一手表产品的用语。参数和交互规则见 [交互契约](docs/UI-INTERACTION.md) 与 [产品应用和组件](docs/PRODUCT-UI-SCOPE.md)。

## 显示与输入

**主表盘**：日常查看时间的主页，也是全局按键回主页的目标。
_避免混用_：应用列表、任意卡片。

**组件环**：主表盘与用户配置的组件页组成的横向循环页面集合。
_避免混用_：固定的心率/活动/系统三页、应用菜单。

**组件页**：按固定布局容纳若干组件的页面，是组件环中除主表盘外的浏览单元。

**组件**：某个应用的摘要及入口，同一应用可以在多个位置出现。
_避免混用_：应用本身、仅改变颜色后产生的新应用。

**变暗态**：屏幕仍显示内容、亮度相对用户设置降低的过渡状态；首次触摸用于提亮。
_避免混用_：低亮度用户设置、息屏。

**息屏态**：手表停止显示画面的产品状态；此名称不代表主控或整机已达到特定功耗。
_避免混用_：深睡、整机断电。

**返回**：回到当前页面的上一级，并保留该上级页面应恢复的浏览位置。
_避免混用_：回主页。

**回主页**：离开当前页面层级，回到主表盘。
_避免混用_：返回上一级、返回应用列表。

**通知预览**：息屏收到新消息后临时显示的独立简略页面，与通知中心及完整详情区分。
_避免混用_：亮屏消息浮窗、离开原应用的导航动作。

**消息浮窗（通知横幅）**：新消息到达时位于屏幕上方的短暂简略提示，不替代当前应用页面。
_避免混用_：通知中心、完整消息详情。

**通知中心**：从主表盘下拉进入、集中浏览收到的通知及其详情的页面。
_避免混用_：通知预览、通知横幅。

**勿扰模式**：约束通知如何打扰用户的设置，与手机是否保持连接分别决定。
_避免混用_：断开蓝牙、睡眠模式。

**睡眠模式**：用户为睡眠时段选择的使用模式，可关联连接、提醒及采样策略。
_避免混用_：主控深睡、睡眠识别结果、勿扰模式。

**控制中心**：从主表盘上滑进入、集中展示状态和常用快捷操作的页面。
_避免混用_：通知中心、完整设置目录。
