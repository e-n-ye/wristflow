# 当前状态

更新：2026-09-25。用户已确认黄山派到货，菜单/应用首版已获真机反馈。本轮 [触摸修正与调度核验](UI-TOUCH.md) 已完成 6/6 主机测试、固件编译、COM5 写入校验与启动采样。用户随后反馈“明显改善”，但认为应用太少、界面单薄，尚不能与 Apple 蜂窝效果相比；持续交互耗时未取得。最新 PR 云端状态以 GitHub 为准。

最新增量：修正无关手势终止滑条输入的问题；菜单同时处理 X/Y，选择页改为滑动/吸附/点击应用，心率/活动/电池改为独立占位页。已确认 LCD 亮度为同步调用、刷新路径存在信号量等待；UI 主循环改为按 LVGL 下一任务时间等待并可被 KEY1 唤醒。蜂窝专项超时后停止继续优化；根据最新改善反馈暂保留蜂窝，应用数量、布局密度与完整缩放效果留待后续完善。原卡片、控制中心、秒表和手电筒保留；真实数据与重启保存尚未接入。

## 最新决定与结果

- [黄山派选型复评](HARDWARE.md) 记录新货源、板载外围、8MB PSRAM/16MB Flash、设计尺寸及版本差异；预算、首版范围和三个月日历不变。
- 默认板型改为 `sf32lb52-lchspi-ulp`；SDK v2.5.1 与两个子模块版本不变，SDK 源码零修改。VS Code 索引改为 Huangshan，三个任务名称与快捷键不变。
- 本地 Hello、BLE、Bringup 编译链接及产物检查通过；基线与交互文档 PR 均通过全新 Windows runner 云端构建并合入 main（`2d01143`）。
- 等板期间对齐产品交互，完成 [交互规格与状态机契约](UI-INTERACTION.md)；按纯黑极简单色大字表盘与通用卡片容器要求，在 `ui/xml/` 建立首批 390×450 LVGL XML 声明并完成语法校验。
- 有限范围的 grill-with-docs 已沉淀 [LVGL Pro 中文说明与 XML 复核](LVGL-PRO.md) 和 [产品术语](../CONTEXT.md)。变暗态首次短按 KEY1 仅提亮并消耗该次按键，下一次才导航。用户选择 Community GUI 预览/导出；现有 Editor 2.0.1 已切换并确认 Community / No Expiry。SDK 和预览均为 LVGL 9.4.0。
- 按用户参考图制作表盘和可复用的信息环基础：`quarter` 165×174、`half` 342×174、`full` 342×362，四种固定布局模板及三种规格的纵向选择页；表盘使用 12 小时制 `10:48` 静态演示和 `hour_1` 至 `hour_12` 资源。[真实预览与中文操作](UI-DEMO.md) 已留档。新增独立 `ui_demo`，PM/BLE/XML runtime 关闭，编译生成 C 和内嵌资源；Bringup 保持原验收用途。主机 `clock_format`/`ui_preview` 两项测试通过，黄山派 UI Demo 本机编译通过，CI 已增加 UI Demo 编译步骤。
- `watch_core` 已有最小导航状态和独立秒表核心；长按选择已接入，重启保存、卡片布局编辑、电源和通知状态仍未实现。当前扩散方向为白灰、整组小时数字、两侧平直毛玻璃，外层与内层宽度约 1:2；动画过程与触发时机尚未决定，不继续预设呼吸动效。

## 已完成

- 主代理完整阅读三份共识与采购材料，记录本轮优先级变化。
- 确认初始项目目录为空，建立本地 Git 仓库；未创建远程仓库。
- 阅读官方 Windows 安装入口，核验独立工具目录及 SDK 锁文件。
- 只读检查旧项目 CI：它绑定 F411、CMake 和旧目录，暂不迁入。
- SDK v2.5.1 和两个子模块已固定；官方工具链安装成功，环境兼容性检查通过。
- Nano R16N16 Hello World 和 BLE peripheral 均实际编译链接成功，SDK 源码零修改。
- 生成配置确认 CO5300、FT6146、390×450；两项基础示例的 PM 和触摸唤醒均关闭。
- 留存首次完整构建日志、配置与哈希；安装包装脚本和 Hello 增量构建已执行验证。
- 旧 MAX30102 代码已只读评估，见 [复用边界](F411-REUSE.md)，没有迁移。
- 已添加 [VS Code 编译入口](VSCODE.md)：Hello/BLE 两个任务与 C/C++ 编译数据库配置，沿用现有工具链，不包含烧录。
- 用户已在 VS Code 手动完成 Hello 编译，退出码和固件哈希与基线一致。针对任务内存表乱码统一了 PowerShell/Python UTF-8，并增加 `BUILD SUCCEEDED` 完成提示。
- 随后两个 VS Code 任务在激活阶段访问 PyPI 失败；已改为复用安装好的 SDK Python，离线执行官方检查和导出。相同任务命令在离线参数与不可用代理条件下均增量构建通过，退出码 0、固件哈希不变，详见 [构建说明](BUILD.md)。
- 用户确认已下单，希望等板期间继续推进，选择先准备上板基础固件；订单 SKU、成交价、附件和实际版本未提供或验证。
- 新增项目自有 `apps/bringup/`：LVGL v9 测试屏、10%～100% 亮度滑块、触点坐标、10s 变暗、15s 熄屏、KEY1 唤醒路径。已实际编译链接成功；PM/Deep Sleep 配置开启，BLE 与触摸唤醒关闭。源码和到货步骤见 [Bringup 实验](BRINGUP.md)。

## 本轮产物

- [构建说明](BUILD.md)：版本、重建命令、产物路径、配置和已知问题。
- [首次构建证据](evidence/2026-09-21)：实际日志、配置与机器可读产物清单。
- `scripts/Install-Sdk.ps1`、`scripts/Build.ps1`：独立环境安装与 hello/ble/bringup 三个构建目标，均不烧录。
- `WristFlow: Build Bringup` VS Code 任务及应用编译数据库索引。构建记录增加应用源文件哈希，SDK 保持零修改。
- [到货验收](ACCEPTANCE.md)：通电前检查、显示触摸、BLE、功耗和扩展清单。
- 首次基线提交 `27ee4d1` 已推送到仓库 `https://github.com/e-n-ye/wristflow`；SDK 保持固定子模块。工作流在独立分支验证，详情见 [开发流程](../CONTRIBUTING.md)。
- 2026-09-21 用户确认先投入最多 2h 建立首次提交、托管、最小云端构建和分支/PR 流程；UI + BLE + PM 留为下一项实验。私有仓库分支保护 API 返回 403 套餐限制后，用户明确改为公开；现已公开并成功启用 main 的 PR 与构建保护，管理员同样受约束，未启用付费。
- 本轮本地三个目标均复编译成功：`artifacts/hello/20260921-224638-539/`、`artifacts/ble/20260921-224703-590/`、`artifacts/bringup/20260921-224722-078/`，固件哈希不变。全新云端环境三目标也成功，配置与基线一致；版本、云端哈希及差异限制见 [工作流证据](WORKFLOW-EVIDENCE.md)。PR #1 的最终状态以 GitHub 为准。

## 尚未验证

黄山派已到货；先前固件的基础显示/触摸/亮度/KEY1 和官方菜单已有用户确认。本轮修正版已写入并启动，用户确认整体体验明显改善；尚无量化拖动帧率，也未逐项确认选择页等所有操作。实物具体版本、重复亮灭屏稳定性、BLE 通知/删除/重连、实际休眠状态、整机电流与续航仍未验证。

## 下一步顺序

1. **黄山派本地最小构建已达到无板结束条件**：三目标产物、配置、失败修复和上板步骤已留档；等待/核对当前 PR 最新云端构建，不把编译计为显示或休眠通过。后续可在 VS Code 复编译自有 Bringup。
2. **当前 UI 任务**：验收触摸修正版的全向菜单、滑动选表盘和连续调亮度；按板上耗时区分绘制与同步等待，核验本分支最新 CI。扩展多级应用前建立最小页面栈与进入/离开/销毁契约；不把加页面栈当作帧率修复。后续接真实时间与持久化，视觉细节继续后置。
3. **真机后续**：继续核对实物版本、触摸边缘、内存容量和重复亮灭屏；新框架单独进行上板验收，不把旧固件结果扩大为本分支已通过。
4. **下一项软件扩展候选**：以已构建 Bringup 与官方 BLE 外设为基础，核查同一固件的 PM + UI + BLE 连接/事件，首先移除/替换当前非 BLE 的 LCPU 睡眠假设，再形成独立联合固件测试。真实连接和电流仍待实测；Gadgetbridge 实验单独计时，最多 8h。
5. **传感器阶段**：基于 F411 评估补齐测量生命周期，再决定迁移和目标模块接入。

本轮只完成了约 4h 时间盒内定义的编译阶段产物，未消耗硬件实验额度，也不延长首次路线评审的日历。

## 未解决的实施风险

- XML 历史样式/参数/字体问题已修复并真实渲染；Community 不含官方 CLI，仍需人工 GUI 导出及截图复核。CI 编译生成 C 不能证明其与最新 XML 同步。静态 `10:48`、样例数据和 12 张小时图不代表真实 RTC、连接或传感器功能；呼吸/伸缩动画与硬件刷新功耗尚未验证。
- 12 张 342×282 的 RGB565 小时图片未压缩像素数据约 2.21 MiB；历史 `hour_seven_data.c` 已不再由官方 CMake 清单引用，并已从 SCons 图片源收集中排除，避免无引用 ARGB8888 数据继续增加固件体积。
- Hello/BLE 基础例程 PM 关闭，Bringup 已开启 PM 但无 BLE；仍没有同一固件下的 UI/BLE/休眠运行证据。
- 黄山派物理 PSRAM 和默认数据分区均为 8MB；不能沿用 Nano 物理 16MB 的资源预期，应用峰值与板上容量待测。
- 官方例程链接有 RWX LOAD segment 警告；未修改链接脚本，运行风险仍待板端验证。
- Bringup 编译包含官方 LVGL `lv_obj_tree.c:274` 的潜在空指针警告；触摸开关是 SDK 异步操作，快速熄屏/唤醒及驱动定时器释放须重点实测。
- 黄山派实际版本、附件与整机厚度待核实；PPG、电池、电机和固定结构仍需选型。充电参数、电池极性、共享电源和屏幕/音频/充电驱动耦合见复评及验收清单。
- 采购等待计入 2026-09-21 至 2026-12-21 的日历；按每周 20h 规划，十二周评审点为 2026-12-14。
